on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - run: ls -R | grep quartz
      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: 'quartz/package-lock.json'
      - run: npm ci
        working-directory: quartz
      - run: rm -rf quartz/public
      - run: find . -name public
      - run: |
          cd quartz
          npx quartz build --directory=../
      - name: Add CNAME
        run: echo "vault.anirudh.foo" > quartz/public/CNAME
      - uses: actions/upload-pages-artifact@v4
        with: { path: quartz/public }

  publish:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write 
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to gh pages
        id: deployment
        uses:  actions/deploy-pages@v4
